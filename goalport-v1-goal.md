# GoalPort V1 开发目标

依据同目录的 [V1 r2 产品设计](2026-08-31-goalport-v1-design-r2.md)，在当前明确的项目工作区实现并验证一个 Windows 纯对话式、多原生 Coding Agent 桌面客户端。GoalPort 暂作项目名。

目标不是再做一个支持三个模型的聊天壳，也不是把开发者的 Goal Skill 做成产品，而是同时解决四项问题：长任务可持续与安全恢复；结果有可核查证据；多个现有订阅的可解释路由；减少用户管理任务、搬运上下文和处理异常的负担。

保留 `Campaign → Task → Attempt`，使用独立于 UI 生命周期的本地控制核心。计划、审计、自动推进、恢复、路由、数据传输和资源策略可配置；权限与证据真实性不能被普通预设削弱。用户继续用 VS Code 编辑代码。

使用本机已有、已登录且实际验证可用的 Claude Code、Codex、Grok Runtime；原生 plugins、skills、hooks、MCP、会话、工具与上下文仍归 CLI。不得复制个人 Harness、改写其状态、绕过其规则、强制安装开发者配置，或把无法使用订阅悄悄替换为付费 API。

技术方向为 Tauri + React/TypeScript + Rust Core + SQLite。具体 Adapter、Host/进程布局和 crate 拆分依据真实证据与现有代码确定，不以空壳数量代替架构。优先复用适当的开源协议和基础组件，保持产品边界，不扩张为 IDE、Git 管理器或通用工作流平台。

按 r2 的 M0–M5 建立真实接入与合成故障验证；关键接口假设先验证，再冻结受影响实现。首个可用链路必须有真实 Runtime 和实际 GUI 使用证据；两个 Runtime 时展示持续性、验证、路由和低干预的最小完整闭环，最终补齐三家与硬化。真实账户、工作目录、对话、日志和配置不得进入源码或公开 fixture。

验收依据 r2 的 23 项场景和实际支持矩阵。修复可继续原责任；真正改派再交接；旧证据不能证明新代码；未知外部效果不得重放；能力、配额和运行结果未知时明确说明。Mock、零范围 exit 0、虚拟 soak、审计叙述和文档自检不能替代各自层级的真实结果。

交付应是可运行 App、相应源码和合成测试、真实范围明确的验证材料及剩余限制，而非只有设计、框架或连接演示。尚未达到三家 Stable V1 时可以交付可用预览并列出缺口，不能伪称完成。现行 Skill 负责开发计划、委派与审计；本目标不另造开发流程，不扩大 Git 提交、外发、发布、删除或显著资源消耗的授权。
