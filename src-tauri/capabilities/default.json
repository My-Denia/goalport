{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimum permissions for the GoalPort desktop shell.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
